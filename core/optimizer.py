"""core/optimizer.py — stub, implemented in Step 6."""
from core.logger import get_logger

log = get_logger(__name__)


def run_optimization(config, results_so_far: list) -> dict:
    log.warning("optimizer: stub — not yet implemented")
    return {}
